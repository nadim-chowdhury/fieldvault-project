{
  "name": "@fieldvault/constants",
  "version": "1.0.0",
  "private": true,
  "description": "Shared constants, enums, and permission maps for FieldVault",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
